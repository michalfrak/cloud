from abc import ABC


# created according to aws watchtower found in code (pr not merged yet)
class CloudLogger(ABC):
    """ Abstract class with definitions of cloud provider logger """

    # TODO: add rest of definitions for logger interface
